# K Means Clustering using TensorFlow


```functions.py``` contains the helper functions used in ```generate_samples.py```. The code generates random samples and aims to group the samples into 3 clusters. 2 features have been selected to suit visual representation of the final position of the centroids using ```MatPlotLib```.

![Clustering image](https://i.imgur.com/nvATIiP.png)